# Optional. The client calls /api on its own origin by default: the Vite dev
# server proxies it to http://localhost:5000, and Vercel rewrites it to the API.
# Set this only to point at an API on another host.
VITE_API_BASE_URL=
